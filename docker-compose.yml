services:
  hermes-medical-bot:
    build: .
    container_name: hermes-medical-bot
    env_file:
      - ./config/.env
    volumes:
      - ./data:/medical/data
      - ./config:/medical/config:ro
    restart: unless-stopped
    command: ["python", "-m", "hermes_medical_agent.bot"]
